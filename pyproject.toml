[project]
name = "playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "bryanmsombra27", email = "bryanjm96@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "django>=6.1",
]

[project.scripts]
playground = "playground:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
